This module will illustrate different options to upload files using the multipart resolver.

Here are the following multipart options:

1) @RequestPart annotation
2) MultipartFile
3) Part (needs Servlet 3.0)

To compile and build, execute: gradle clean build

Then deploy to a application container and you can invoke the following URL in your browser:

http://localhost:8080/evidence-cloud
