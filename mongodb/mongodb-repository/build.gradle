apply plugin: 'spring-boot'

jar {
    archivesBaseName = 'mongodb-repository'
    version = '1.0.0-SNAPSHOT'
}

dependencies {
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-data-mongodb'
}
