apply plugin: 'spring-boot'

jar {
    archivesBaseName = 'springcloud-stream-source'
    version = '1.0.0-SNAPSHOT'
}

dependencies {
    compile("org.springframework.cloud:spring-cloud-stream:${springCloudStreamVersion}")
    compile("org.springframework.cloud:spring-cloud-starter-stream-kafka:${springCloudStreamVersion}")
}
